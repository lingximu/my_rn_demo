import Toast, {DURATION} from 'react-native-easy-toast'

<Toast ref="toast"
                positionValue={200}
                fadeInDuration={650}
                fadeOutDuration={600}
                opacity={0.8}/>
            </ScrollView>
